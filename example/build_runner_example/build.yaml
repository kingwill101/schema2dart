targets:
  $default:
    builders:
      schema2dart|schema_builder:
        generate_for:
          - lib/schemas/**.json
        options:
          emit_validation_helpers: true
          include_globs:
            - lib/schemas/**.json
